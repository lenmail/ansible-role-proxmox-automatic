---
# .ansible-lint

profile: production

exclude_paths:
  - .cache/
  - .ci-roles/
  - examples/roles/

skip_list:
  - yaml[line-length]  # We use yamllint for this
  - name[casing]       # Allow descriptive task names

warn_list:
  - experimental       # Experimental features as warning
